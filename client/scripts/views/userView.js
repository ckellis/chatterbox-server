var UserView = Backbone.View.extend({
  el: '.userinfo',

  initialize: function() {
    // this.set('messages', new messagesView({collection: Messages}));
    // this.render();
  },

  render: function() {
  }

});
